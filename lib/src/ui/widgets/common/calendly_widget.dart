import 'package:flutter/material.dart';

class CalendlyWidget extends StatelessWidget {
  final String url;
  const CalendlyWidget({super.key, required this.url});

  @override
  Widget build(BuildContext context) {
    // ئەڤ پشکە ب تەمامی هاتە لادان دا چو ناڤ و ڕەنگێن سپی نەمینن
    // SizedBox.shrink() ناهێلیت چو جهی د مالپەری دا بگریت
    return const SizedBox.shrink();
  }
}